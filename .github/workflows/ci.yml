name: CI

on:
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Compilar sin avisos
        run: make CFLAGS='-Wall -Wextra -Werror -g -std=c11 -D_GNU_SOURCE'
      - name: Ejecutar pruebas
        run: make test
